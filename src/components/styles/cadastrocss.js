import react from 'react';
import styled from 'styled-components';

export const FundoCad = styled.div`
  width: 420px;
  background: rgba(0, 0, 0, 0.5);
  color: white;
  border-radius: 10px;
  padding: 30px 40px;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.7);
  position: relative;
  z-index: 1;
  text-align: center;
  backdrop-filter: blur(10px);
  margin-top: 90px;
  
`;